# Editable runtime dependencies (equivalent to install_requires)
# Add direct runtime dependencies here, as well as implicit dependencies
# with constrained versions.
#
# After editing, update requirements/main.txt by running:
#     make update-deps

# These dependencies are for FastAPI including some optional features.
fastapi!=0.89.0
aiofiles
fastapi-sqlalchemy
python-multipart
starlette
uvicorn[standard]

# Other dependencies.
arrow
click
httpx
python-dateutil
safir[arq,redis]>=4.0
structlog
pydantic
markdown-it-py[linkify,plugins]<3.0.0 # for mdformat compatibility
pyyaml
mdformat
mdformat-gfm
gidgethub
